const String THEME_LIGHT = 'Light';
const String THEME_DARK = 'Dark';

class Settings {
  String theme = THEME_LIGHT;
  int numPokemons = 10;

  void load() {
  }

  void save() {
  }
}

var settings = Settings();
